meta {
  name: create user
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/api/v1/users
  body: json
  auth: inherit
}

body:json {
  {
    "username":"Eugene"
  }
}

settings {
  encodeUrl: true
}
